import 'package:calculation_ocr/features/calculation/domain/entities/calculation.dart';

class CalculationModel extends Calculation {
  const CalculationModel({required super.expression, required super.result});
}
